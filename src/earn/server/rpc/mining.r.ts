/**
 * 
 */
// export const mining = (itemQuery:ItemQuery) => {

// }
const a = 0;

// #[rpc=rpcServer]
export const doTest = (uid: number) => {
    console.log('item query in !!!!!!!!!!!!');

    return;
};